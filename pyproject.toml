[project]
name = "agendalo-workspace"
version = "1.0.0"
description = "Workspace for Agendalo modules"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["modules", "tests"]

